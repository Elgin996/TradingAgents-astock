# ETF 数据源可行性验证

> 状态：阶段 0 已完成；阶段 1 获准启动（用户填写缺失的跟踪指数代码）  
> 验证日期：2026-08-07  
> 样本：`510300`（宽基）、`159915`（创业板宽基）、`512480`（行业）、`588000`（科创板宽基，20% 涨跌幅候选）

本文只记录已经实际验证的结果。未验证、没有稳定来源或缺少授权结论的字段不允许进入 ETF 分析能力清单。

## 已验证结果

| 字段 | 主源 | 备用源 | 历史 | 样本覆盖 | 限流与稳定性 | 授权风险 | 不可得时的处理 |
|---|---|---|---|---:|---|---|---|
| 实时 ETF 行情（价格、换手率） | 腾讯 `https://qt.gtimg.cn/q=sh510300,...` | 东财 push2 `api/qt/stock/get?secid={market}.{code}` | 仅快照 | 4/4 | 腾讯成功；东财必须沿用全局节流 | 需在产品使用前确认 | 不可用时不启用实时流动性指标 |
| ETF 名称、管理人、费率、份额、规模、跟踪标的 | 天天基金基金档案 `https://fundf10.eastmoney.com/jbgk_{code}.html` | 东财 `FundBaseTypeInformation.ashx`（名称、基金类型、管理人、当前净值日期、规模） | 档案为最新披露快照；尚未验证完整历史 | 4/4 支持样本 | 公开 HTML，已解析 4 列资料表；需要限流、缓存和页面结构监控 | Choice 数据，需确认产品使用许可 | 低频描述字段可标记缺失；不得伪造历史份额 |
| 证券类型、ETF 资产类别、交易所 | mootdx `stocks(market=0/1)`（交易所成员资格）+ 天天基金档案（基金类型、全称、跟踪标的） | 无已验证独立源 | 当前清单快照 | 4/4 支持、5/5 拒绝样本 | mootdx 整表拉取约 16 秒，不能在输入时逐只调用；需要按交易日缓存 | mootdx/Choice 的产品使用许可待确认 | 主数据任一侧不可用时阻止开始分析，不根据代码前缀判定 |
| ETF 日 K 和成交量 | 新浪 `CN_MarketData.getKLineData?symbol={prefix}{code}` | 东财 push2his（见下行） | 约 800 根日线 | 4/4 | 4/4 返回日线；**无成交额字段** | 需确认 | 可支持技术指标；无 Amount 时不得启用依赖成交额的流动性指标 |
| ETF 日 K + 成交额 | 东财 push2his `api/qt/stock/kline/get?secid={market}.{code}&fields1=f1,f2,f3,f4,f5,f6&fields2=f51,f52,f53,f54,f55,f56,f57&klt=101&fqt=1` | 新浪（无 Amount） | 可翻页历史 | 4/4（`510300`/`159915`/`512480`/`588000`） | 字段映射：`f51`日期 `f52`开 `f53`收 `f54`高 `f55`低 `f56`成交量 `f57`成交额；必须走全局 `_em_get` 节流（`EM_MIN_INTERVAL`） | 需确认产品使用许可 | **主源**：实现走 `_em_kline_with_amount`；有 Amount 时启用 `liquidity_metrics`；push2his 失败时降级新浪并关闭依赖成交额的流动性结论 |
| ETF 日 K（mootdx） | mootdx `client.bars(symbol=..., frequency=9)` | 新浪 | 未验证 | 0/4 | 4 个样本均返回空 DataFrame | 不适用 | 不能作为 ETF 日 K 主源；现有 mootdx-first 实现必须在 ETF 模式中绕开 |
| 基金份额、净资产规模 | 天天基金 `FundArchivesDatas.aspx?type=gmbd&mode=0&code={code}` | 基金档案基本概况页的最新披露值 | 季度序列 | 4/4 | 4/4 返回申购、赎回、期末总份额及期末净资产；返回 JavaScript 包装的 HTML 表格 | Choice 数据，需确认产品使用许可 | 可启用时必须使用披露期，不得称为实时份额或实时资金流 |
| 历史基金份额净值 | 天天基金 `api.fund.eastmoney.com/f10/lsjz?fundCode={code}&...` | 无已验证独立源 | 可翻页历史 | 4/4 | 4/4 返回 `FSRQ`、`DWJZ`、`ACCUM` 和日增长率；JSONP，需要解析回调包装 | 需确认 | 仅用于已公布净值；不得改名为 IOPV 或盘中折溢价 |
| 基金公告 | 天天基金 `api.fund.eastmoney.com/f10/JJGG?fundcode={code}&pageIndex=1&pageSize=...&type=0` | 基金公司公告页，未验证 | 可翻页历史 | 4/4 | 4/4 返回公告日期、标题、类别与公告 ID；必须带 Referer 和正常 User-Agent | 需确认 | 无法取得时移出基金公告能力，保留可追溯的指数/政策新闻 |
| IOPV | 无公共、无需授权的可用读接口 | 无 | 无 | 0/4 | 上交所 2026 规范说明 IOPV 通过独立行情渠道发布；上传 REST 接口仅供获资质机构使用 | 交易所行情权限 | 关闭阶段 1.5；不输出盘中折溢价 |
| 每日 PCF 申赎清单 | 无公共、稳定、无需授权的下载接口 | 无 | 无 | 0/4 | 上交所和深交所规范都说明 PCF/ETF 定义文件通过面向市场参与者的文件或专线渠道传输 | 交易所文件/行情权限 | 关闭阶段 2 的 PCF、申赎限制与现金替代分析 |
| 指数成分及权重 | 无已验证、许可清晰的全覆盖来源 | 无 | 无 | 0/4 | 中证系列的公开免费接口不提供权重；权重数据有指数公司授权限制 | 指数公司授权 | 关闭阶段 2 的指数暴露、集中度和聚合估值 |
| 指数全收益序列 | 无免费、稳定的公开来源 | 无 | 无 | 0/4 | 可获得价格指数行情，但未获得总收益口径；价格指数不能替代高股息 ETF 的跟踪差异 | 指数公司授权 | 关闭阶段 2 的跟踪差异与跟踪误差 |
| 交易所 | mootdx `stocks(market=0/1)` 清单归属 | 东财 `secid` 路由验证 | 当前快照 | 4/4 | 样本分别落在正确市场 | 需确认 | 仅作为候选映射；不能单独取代证券主数据 |

路由探测确认：`510300` 使用 `sh510300` / `1.510300`，`159915` 使用 `sz159915` / `0.159915`；4 个样本的腾讯与东财均返回相应基金名称。

已新增 `tradingagents/dataflows/security_master.py`，将两个独立来源组合为只读验证适配层：mootdx 决定交易所成员资格，天天基金档案决定基金类型与跟踪标的。它在真实调用中将 `510300`、`159915`、`512480` 和 `588000` 分类为 `domestic_equity_etf`，并正确拒绝国债 ETF（`511010`）、黄金 ETF（`518880`）、跨境 ETF（`513500`）、LOF（`161005`）及 REIT（`508000`）。这不是代码前缀推断。

## 未通过或未完成的必需字段

| 字段 | 当前结论 | 阶段 1 影响 |
|---|---|---|
| 供应方标识的跟踪指数代码 | 基金档案提供跟踪指数名称，但没有可审计、供应方限定的指数代码。用户已决定在此唯一缺口由页面收集代码及供应方，并以 `user_supplied` 状态持久化。 | 不再阻断阶段 1；用户输入不得改变系统识别出的类型、资产类别或交易所。 |
| 管理人、成立日期、跟踪指数、费率、申赎状态 | 档案已经提供管理人、成立日期、跟踪指数名称与费率；`FundBaseTypeInformation.ashx` 提供状态。仍未验证其完整性、时点语义及产品许可。 | 仅可用于阶段 0 验证，尚不进入运行时能力清单。 |
| 基金份额、规模及其历史序列 | 已验证季度历史来源；尚未验证日频份额来源。 | 阶段 1 可将季度披露作为滞后资料，不能生成“当日净申购”或“实时份额”结论。 |
| 指数调仓公告 | 尚未验证每个指数供应方的结构化接口、历史覆盖与许可。基金公告已验证。 | `index_news_policy` 只能先使用带日期的通用新闻和基金公告；指数调仓公告能力不能启用。 |
| IOPV | 交易所规范确认需要独立行情权限；当前接入源没有可用读接口。历史基金份额净值已验证，但不是 IOPV。 | 阶段 1.5 已关闭。 |
| PCF 申赎清单 | 交易所规范确认其面向市场参与者的文件/专线交付方式；没有可用公共读接口。 | 阶段 2 的 PCF 部分已关闭。 |
| 指数成分与权重、全收益指数序列 | 权重存在指数公司许可限制；全收益序列无稳定免费来源。 | 阶段 2 的暴露和跟踪质量已关闭。 |
| `price_limit_pct` 推导 | 尚未验证可重复的指数成分板块判断规则。 | 只能在有正式来源或明确保守假设时展示，不能作为身份字段。 |

## 阶段结论

阶段 -1 已完成并有回归测试：沪市 `5xxxxx` ETF 的 Tencent、东财和新浪路由已改为上交所；名称映射已纳入 `5xxxxx` 与 `159xxx` ETF。

阶段 0 的调查和裁剪结论已完成：

1. 阶段 1.5 与阶段 2 关闭：IOPV、PCF、指数权重及指数全收益序列不具备当前项目可用的公共、稳定且许可明确的数据源。
2. 阶段 1 获准启动：用户已决定在证券主数据已确认 ETF 身份、交易所、资产类别和跟踪指数名称后，可以填写缺失的跟踪指数代码及供应方。该值必须持久化为 `user_supplied`，且不能改变系统识别的身份字段。
3. 后续如接入可审计的证券主数据源，应以主数据替换此辅助输入；不得静默覆盖已冻结的历史任务。常见宽基/主题指数已由本地 `index_catalog` 预填（来源 `catalog`），未命中目录时仍走用户填写。
4. Choice / mootdx 产品使用许可为运营确认项，不纳入工程完成定义。
