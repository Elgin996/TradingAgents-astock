# 境内股票 ETF 分析模式完整方案

> 文档状态：阶段 -1、0、1 已完成；阶段 1.5 与 2 因阶段 0 数据结论关闭；阶段 3 可核验子集已交付；可选残留（历史徽标、标题文案、跟踪指数目录映射）已完成  
> 适用项目：TradingAgents-Astock  
> 范围：仅支持跟踪境内 A 股指数的场内股票 ETF  
> 前置条件：阶段 -1 路由修复合入，且阶段 0 数据源验证出具书面结论

## 1. 结论与总体思路

项目应在保留现有个股分析流程的基础上，新增独立的“境内股票 ETF 分析模式”。ETF 模式不能只在页面上增加开关，也不能仅通过跳过基本面、龙虎榜和解禁分析来实现。标的识别、数据路由、分析师组合、质量门控、多空辩论、交易决策、历史记录和报告模板都必须理解当前标的是 ETF。

完整链路如下：

1. 由证券主数据识别用户输入对应的证券类型与交易所，向用户回显识别结果并确认，建立统一的标的信息档案。
2. 根据证券类型生成能力清单和交易规则。
3. 采集 ETF 行情、份额规模、指数及公告数据。
4. 运行 ETF 专用分析流程。
5. 使用结构化质量门控检查时点、来源、口径和缺失项。
6. 输出 ETF 专用报告，再进入多空辩论和风险决策。
7. 将标的类型、数据来源、报告版本和质量结论一并持久化。

第二阶段不承诺提供“实时基金实际持仓”。原“成分股与持仓分析”应改名为“指数暴露与跟踪质量分析”，严格区分指数权重、每日申购赎回清单和滞后的定期报告持仓。

本方案的可行性完全取决于阶段 0 的数据源验证结论。IOPV、PCF 申赎清单、指数成分权重和指数全收益序列在当前接入的数据源（mootdx / 腾讯 / 东财 push2 / 新浪 / 同花顺）中均无现成来源，阶段 0 有权据此裁剪阶段 1.5 与阶段 2 的范围。在阶段 0 出具字段级数据源结论之前，不应开始功能开发。

## 2. 支持范围

### 2.1 纳入范围

- 在上海证券交易所或深圳证券交易所上市交易。
- 产品类型经证券主数据确认属于 ETF。
- 跟踪标的是境内 A 股股票指数。
- 包括宽基、行业、主题、风格、策略及红利类股票 ETF。
- 支持普通 10% 涨跌幅 ETF，以及适用 20% 涨跌幅的股票 ETF。

### 2.2 明确排除

- 跨境 ETF、QDII ETF 和港股 ETF。
- 债券 ETF、黄金 ETF、商品期货 ETF、货币 ETF。
- LOF、封闭式基金、REITs、ETF 联接基金。
- 场外基金。
- 主动管理型 ETF；如未来支持，应单独立项，因为其持仓透明度和分析逻辑不同。
- 无法确认产品类型或跟踪标的的证券。

识别到排除类型时，系统应明确提示“当前版本不支持该类型”，不得自动降级为个股分析。

## 3. 建设目标与非目标

### 3.1 建设目标

- 避免把公司财务、管理层、解禁和龙虎榜逻辑误用于 ETF。
- 给出可核验、带来源和数据日期的 ETF 分析报告。
- 支持宽基、行业、主题和策略 ETF 的技术面、流动性、折溢价、指数暴露、政策新闻和跟踪质量分析。
- 对数据缺失、披露滞后和口径不一致进行显式降级。
- 不影响现有个股流程和已有历史报告的读取。

### 3.2 非目标

- 不预测 ETF 的实时真实持仓。
- 不根据季度持仓推导当前分钟级净值或成分股贡献。
- 不提供套利下单、自动交易或申购赎回执行。
- 不把模型生成内容当作原始数据。
- 不保证每个 ETF 都能获得 IOPV、历史份额或完整指数权重；缺失时必须报告不可得。

## 4. 核心设计原则

1. **系统识别、用户确认**：分析模式由证券主数据识别决定，不由用户预先声明。用户看到识别结果后确认开始；只有在主数据无法确认类型，或已确认 ETF 但无法取得跟踪指数代码时，才需要用户介入。
2. **交易所优先于代码前缀**：代码前缀只能作为快速提示，不能作为最终依据。
3. **结构化数据优先于 LLM**：公式计算、日期对齐和质量评级先由程序完成。
4. **同一时点才能比较**：价格、IOPV、净值、份额和指数数据必须保留各自的 `as_of`。
5. **来源可追溯**：每项关键数据必须记录来源、抓取时间、单位和状态。
6. **不伪造精度**：滞后持仓只作为历史参考，不进入实时交易评分。
7. **按能力路由**：分析流程由可用能力决定，而不是强制维持七个固定角色。
8. **向后兼容**：缺少新字段的旧报告按个股旧版格式只读展示。

## 5. 当前项目的主要缺口

### 5.1 证券识别和行情路由

- 中文名称映射在 `a_stock.py` 构建时按 `^(?:[036]\d{5}|8\d{5}|92\d{4})$` 过滤，`5xxxxx`、`159xxx` ETF 被直接丢弃，无法通过名称解析。mootdx `client.stocks(market=...)` 本身返回 ETF，属于过滤条件过窄，修复成本很低。
- `_get_prefix` 仅对 `6`/`9` 返回 `sh`，其余落入 `sz`，因此 `510300` 在新浪被拼成 `sz510300`、在东财被拼成 secid `0.510300`。这是当前已存在的缺陷，与 ETF 模式无关；`trading_graph.py` 的 Yahoo 代码路径已正确将 `5` 视为上海，说明代码内部口径本身不一致。
- 数字代码输入只经过 `safe_ticker_component` 的路径安全校验，没有证券类型校验，任意安全字符串可能进入个股流程。

前两项应作为独立缺陷先行修复，不纳入 ETF 模式的交付范围（见阶段 -1）。

### 5.2 分析流程写死

- Web 默认运行固定的七位个股分析师。
- Graph、ToolNode、ConditionalLogic、进度展示和报告展示使用固定分析师键。
- 质量门控的 `REPORT_FIELDS` 固定登记上述七类报告，但评分时已按 `selected_analysts` 过滤，因此并非完全写死；缺的是“按能力清单登记报告类型”的能力，而不是过滤机制本身。
- 多空辩论、研究经理、交易员、风险辩手和投资组合经理均包含个股专属提示词。
- 收益回顾统一以沪深 300 作为基准，不能适用于所有 ETF。

### 5.3 数据语义不适配

- 现有 `get_fund_flow` 表示二级市场订单资金流，不代表 ETF 净申购或份额资金流。
- 现有 K 线输出重点是 OHLCV，ETF 流动性还需要成交额、换手、成交连续性等信息。
- 财务报表、盈利预测、行业横向财务比较、龙虎榜和解禁工具不应暴露给 ETF 分析师。

## 6. 总体架构

### 6.1 标的信息档案 `InstrumentProfile`

每次分析开始前生成并冻结一份标的信息快照。字段分为两类，失败时的处理方式不同。

**身份字段（identity）：必须由证券主数据确认，无法确认即终止分析。**

| 字段 | 含义 |
|---|---|
| `symbol` | 纯证券代码 |
| `exchange` | `SSE` 或 `SZSE` |
| `security_type` | `stock` 或 `etf` |
| `etf_asset_type` | 当前版本必须为 `domestic_equity` |
| `fund_name` | 基金正式名称 |
| `tracking_index_name` | 跟踪指数名称 |
| `tracking_index_code` | 跟踪指数代码及供应方标识；主数据缺失时由用户填写并标记为 `user_supplied` |
| `profile_as_of` | 档案生效日期 |
| `source` | 标的信息来源 |

**规则与描述字段（derived）：允许缺失、推导或使用常量默认值，但必须带 `status`，不得因缺失而终止分析。**

| 字段 | 含义 | 缺失时的处理 |
|---|---|---|
| `fund_manager` | 基金管理人 | 标 `missing`，不影响分析 |
| `listed_date` | 上市日期 | 标 `missing`；影响历史窗口时降低质量评级 |
| `currency` | 当前范围固定为人民币 | 常量 |
| `turnaround_rule` | 境内股票 ETF 为 T+1 | 常量 |
| `lot_size` | 最小交易单位；境内 ETF 统一为 100 份 | 常量，无需数据源 |
| `price_limit_pct` | 10% 或 20% | 见下 |

`price_limit_pct` 没有任何现成公开接口按 ETF 逐只发布。实现上只能由跟踪指数的成分板块推导（成分主要来自科创板或创业板的股票 ETF 适用 20%，其余 10%），因此该字段必须携带 `status = "derived"` 并在报告中说明推导依据。它是交易约束提示，不是分析结论的输入，不允许因为无法确定而终止分析——此时按 10% 保守取值并标注 `assumed`。

身份字段确认失败时终止分析，不允许模型猜测，也不允许根据代码前缀强行推断。例外是：证券主数据已确认产品类型、资产类别、交易所和跟踪指数名称，但缺少供应方标识的指数代码时，页面允许用户填写指数代码及供应方；该字段必须保留 `user_supplied` 状态、原始值和确认时间，不能伪装成主数据结果。

### 6.2 统一数据记录 `DataPoint`

所有关键数据项采用相同元数据：

| 字段 | 含义 |
|---|---|
| `value` | 原始值或结构化内容 |
| `unit` | 元、份、亿元、百分比等 |
| `as_of` | 数据代表的业务时点 |
| `retrieved_at` | 系统抓取时间 |
| `source` | 来源名称及接口 |
| `status` | `ok`、`stale`、`missing`、`unsupported`、`error` |
| `notes` | 口径、估算方法和限制 |

`as_of` 与 `retrieved_at` 不得混用。历史分析只能使用分析日当时已经公开的数据，防止未来数据污染。

### 6.3 能力清单 `AnalysisCapabilities`

境内股票 ETF 默认启用：

- `price_history`
- `technical_indicators`
- `liquidity_metrics`
- `fund_profile`
- `shares_and_aum`
- `index_news_policy`
- `nav_iopv`（阶段 1.5，取决于阶段 0 结论）
- `premium_discount`（阶段 1.5，取决于阶段 0 结论）
- `index_exposure`（阶段 2，取决于阶段 0 结论）
- `tracking_quality`（阶段 2，取决于阶段 0 结论）

能力清单是运行时事实，不是静态常量。某项能力所依赖的数据源在本次运行中不可用时，该能力应从清单中移除，质量门控和报告模板据此调整必采项，而不是保留栏目并填入空值。

默认禁用：

- `company_financials`
- `profit_forecast`
- `insider_transactions`
- `dragon_tiger_board`
- `lockup_expiry`
- `company_management`
- `company_competitive_moat`

## 7. 标的识别与页面交互

### 7.1 页面入口

侧边栏沿用现有布局和现有输入框，不增加模式勾选框、下拉框或新的操作步骤。用户输入代码或名称后，系统在现有的识别回显位置展示主数据识别结果：

```text
证券代码或名称
[ 510300                              ]
识别为 ETF · 华泰柏瑞沪深300ETF · 上交所
跟踪沪深300 · T+1 · 涨跌幅 10%（推导）

[ 开始分析 ]
```

个股保持现有体验不变：

```text
证券代码或名称
[ 600519                              ]
识别为 股票 · 贵州茅台 · 上交所

[ 开始分析 ]
```

交互规则：

- 分析模式由识别结果决定：`security_type = "stock"` 时 `analysis_mode = "stock"`，`security_type = "etf"` 且 `etf_asset_type = "domestic_equity"` 时 `analysis_mode = "etf"`。
- 输入支持证券代码和完整名称。
- 识别回显使用现有 `success` / `caption` 控件，不新增卡片或页面。ETF 额外展示跟踪指数、T+1 和涨跌幅限制及其推导状态。
- 识别结果与分析模式在“开始分析”之前对用户可见，用户按下开始即视为确认。不增加第二个确认按钮。
- 识别失败或类型无法确认时，禁用“开始分析”并说明原因，不提供“忽略并继续”入口。
- 识别到排除类型（跨境、债券、黄金、商品、货币、LOF、REITs、联接基金、主动管理型）时，禁用开始并明确提示“当前版本不支持该类型”，不得降级为个股分析。

不设置模式勾选框，理由是：主数据是分析模式的唯一事实来源，用户预先声明的模式不携带任何系统尚不知道的信息，只会引入一类“用户猜错→被拦截→要求用户改回系统已知答案”的错误状态。

### 7.2 证券类型识别流程

1. 读取原始输入，规范化代码或名称，保留原始输入用于错误提示。
2. 从证券主数据查询证券代码、交易所和真实产品类别。
3. 类型为普通股票时，`analysis_mode = "stock"`，走现有流程。
4. 类型为 ETF 时，继续判定资产类别；确认为境内股票 ETF 则 `analysis_mode = "etf"`。
5. 类型为 ETF 但属于跨境、债券、黄金、商品、货币等排除类别时，明确拒绝。
6. 类型无法确认时终止，不得根据代码前缀或输入形态推断。
7. 已确认境内股票 ETF 但缺少跟踪指数代码时，显示跟踪指数代码及供应方输入框；用户填写并点击开始后，字段标记为 `user_supplied`。
8. 识别通过后生成 `InstrumentProfile`，回显给用户，等待用户点击开始。
9. 用户点击开始后冻结 `InstrumentProfile` 并随请求一同下发，运行期间不再重新识别。

主数据不可用（接口超时、限流、缓存缺失）与主数据明确返回“类型未知”应区分处理：前者提示重试，后者提示不支持。两种情况都不启动分析。

### 7.3 请求、恢复与模式持久化

前端启动请求至少包含：

```text
ticker
trade_date
analysis_mode
instrument_profile
fresh
```

规则：

- `analysis_mode` 必须进入运行配置、初始状态、历史记录和断点身份。
- 暂停或失败后恢复任务时，使用任务保存的 `analysis_mode` 和 `instrument_profile`，不重新执行识别。主数据在此期间发生变化不应改变正在运行任务的模式。
- 同一代码、同一日期若识别模式与旧断点保存的模式不一致（例如主数据修正、或代码被重新分配），必须清除或隔离旧断点，不能跨模式恢复。
- 页面重跑不能丢失正在运行任务的模式；进度和报告展示以任务状态为准，而不是以当前输入框内容为准。

### 7.4 前端最小改动约束

- 不新增页面、导航、详情卡片或新的操作步骤；仅当已识别 ETF 缺少跟踪指数代码时，在既有识别回显处显示内联输入框。
- 保留现有日期、数据起始日期、模型配置、开始、暂停、恢复和停止控件。
- 保留现有进度条、报告折叠面板、多空/风控 Tab 和下载按钮的视觉结构。
- 进度组件只增加按 `analysis_mode` 选择阶段名称的配置，不重写展示组件。
- 报告组件只增加按 `analysis_mode` 选择栏目名称和字段的适配层，不复制整套页面。
- 历史列表显示 `[个股]` / `[ETF]` 徽标；ETF 可附跟踪指数名称。
- 页面标题为「A股个股与ETF多Agent投研系统」。
- 常见跟踪指数代码由本地可审计目录 `index_catalog` 预填（来源标注 `catalog`）；未命中时仍由用户填写 `user_supplied`。目录结果不得伪装成东财主数据，也不得回写已冻结历史任务。
- Choice / mootdx 等第三方数据的产品使用许可属于运营确认项，不在工程交付范围内。

### 7.5 缓存策略

- 标的信息可按交易日缓存。
- 基金名称、管理人等低频字段可长期缓存，但保留生效日期。
- 产品类型、上市状态、交易规则和跟踪指数在每次新分析前进行轻量校验。
- 识别是输入框的同步路径，必须足够快：证券类型与交易所的映射表应整体预取并按交易日缓存，不允许每次输入触发一次远端逐只查询。
- 指数级数据（指数行情、指数新闻与政策、指数成分权重、指数聚合估值）按 `tracking_index_code` 缓存，不按 ETF 代码缓存。同一指数的多只 ETF 必须复用同一份指数数据与指数分析结果，否则跟踪同一宽基的十余只 ETF 会重复产生完全相同的检索与推理开销。

## 8. ETF 数据模型与口径

### 8.1 基金基础资料

- 基金名称、代码、交易所。
- 跟踪指数名称及代码。
- 基金管理人、上市日期、成立日期。
- 管理费、托管费及其他可公开核验的固定费率。
- 最新基金份额和基金规模，均须带日期。
- 申购、赎回是否开放以及限制状态。

### 8.2 行情和技术数据

- 开、高、低、收、成交量、成交额。
- 日涨跌幅和区间累计涨跌幅。
- 5/20 日平均成交量和平均成交额。
- 波动率、ATR、均线、MACD、RSI、布林带等。
- 支撑位和阻力位。
- 相对跟踪指数的区间超额收益。

技术指标仍可复用，但提示词必须称为 ETF 或指数工具，不得引用公司经营逻辑。

### 8.3 流动性数据

第一阶段至少计算：

- 当日成交量和成交额。
- 5/20/60 日平均成交额。
- 成交额分位数或相对历史位置。
- 零成交或异常低成交天数。
- 换手率；如果份额口径不可靠，则标注不可得。
- 可获得盘口数据时增加买卖价差和盘口深度，否则不由日 K 线估算。

### 8.4 净值、IOPV 与折溢价（阶段 1.5，前置依赖阶段 0）

定义：

- 盘中折溢价率 = `(市场价格 - 同时点 IOPV) / 同时点 IOPV × 100%`。
- 收盘折溢价率 = `(收盘价 - 收盘 IOPV) / 收盘 IOPV × 100%`。

**可得性约束（必须先承认，再设计）**：现有数据源中，IOPV 只能以实时快照形式取得，没有历史序列。这意味着：

- 只有当 `trade_date` 等于当前交易日、且在交易时段内运行时，才可能取得可与价格同时点比较的 IOPV。
- 对任何回溯日期的分析，盘中折溢价在技术上不可计算。此时能力清单中不应包含 `premium_discount`，报告中不出现该栏目，而不是填入一个用当日快照与历史价格拼出的数字——那是本方案中最容易产生错误结论的一类操作。
- 收盘折溢价需要收盘 IOPV 或当日基金份额净值，两者的发布时间均晚于收盘，取决于阶段 0 能否找到稳定来源。
- 回溯分析可退化为“收盘价相对最近已公布份额净值的偏离”，必须使用该名称，标注净值日期与滞后天数，且不得进入交易评分。

规则：

- IOPV 和市场价格必须属于同一交易日及同一可比时点，时间戳缺失即视为不可比。
- 仅有 T-1 基金净值时，可展示“价格相对最近公布净值偏离”，但不能命名为实时折溢价。
- IOPV 不可得、时间戳不明或明显异常时，状态为 `missing` 或 `error`，能力清单中移除 `premium_discount`。
- 折溢价只作为风险和交易成本提示，不单独生成买卖结论。
- 阶段 0 若无法确定稳定的 IOPV 或当日净值来源，本节整体不实施；阶段 1 的 ETF 报告不含折溢价栏目，并在“数据不可得”一节写明原因。

### 8.5 份额、规模与资金流

必须区分三种概念：

1. **二级市场订单资金流**：买卖成交行为；沿用现有资金流接口前必须验证该接口支持 ETF，并明确标注口径。
2. **基金份额变化**：份额增加或减少，是观察一级市场申赎方向的主要公开代理。
3. **基金规模变化**：同时受份额变化和净值涨跌影响，不能直接解释为净申购。

基础计算：

- 份额变化率 = `(本期份额 - 上期份额) / 上期份额`。
- 规模 = `基金份额 × 对应日期基金份额净值`。
- 估算申赎金额可由份额变化乘以代表性净值计算，但必须标注为估算值，不得冒充官方净申购金额。

### 8.6 新闻、政策和公告

检索对象从“公司”改为：

- 跟踪指数名称和代码。
- 指数覆盖行业与主题。
- 对指数权重影响较大的政策。
- 指数编制机构的调仓和规则变更公告。
- 基金管理人的基金公告。
- ETF 申购赎回限制、停复牌、折溢价风险提示和基金合同重大变更。

普通成分股新闻只有在能说明对指数整体影响时才纳入，避免把单只小权重股票新闻夸大为 ETF 核心事件。

## 9. 滞后持仓与第二阶段设计

### 9.1 三种数据必须分栏展示

| 数据类型 | 定义 | 可用于什么 | 不可用于什么 |
|---|---|---|---|
| 指数成分及权重 | 指数设计和当前公开权重 | 行业、主题、集中度、风格分析 | 不能声称是基金当前真实仓位 |
| 每日 PCF | 当日申购赎回篮子及现金替代信息 | 观察申赎操作篮子、替代和限制 | 不能等同基金完整资产组合 |
| 定期报告实际持仓 | 报告期末基金真实披露持仓 | 历史偏离、复制方式验证 | 不能用于当前精确持仓和实时归因 |

### 9.2 第二阶段正式名称

“指数暴露与跟踪质量分析”，包括：

- 跟踪指数前十大权重股。
- 行业分布和主题暴露。
- 前 5/10 大权重集中度及 HHI 等集中度指标。
- 指数编制、选样、加权和定期调仓规则。
- 最新和即将生效的指数调仓公告。
- 指数 PE、PB、股息率等聚合估值。
- ETF 净值收益相对指数收益的跟踪差异。
- 规定窗口下的跟踪误差。
- 定期报告实际持仓与同期指数权重的历史偏离。
- PCF 现金替代比例、申赎限制和异常篮子变化。

### 9.3 持仓使用规则

- 所有持仓表标题必须写明“指数权重”“PCF 篮子”或“定期报告实际持仓”。
- 定期报告持仓始终标记为历史参考，不参与短期交易评分。
- 禁止用最新季度持仓推导分析日的实时权重。
- 禁止把 PCF 股票数量直接换算成基金完整仓位比例，除非存在完整、可验证的计算口径。
- 数据日期距离分析日较远时，界面显著显示滞后天数。

### 9.4 跟踪质量口径

- 日跟踪差异 = `ETF 复权净值日收益率 - 跟踪指数全收益口径日收益率`。
- 区间跟踪差异 = 两者区间累计收益率之差。
- 年化跟踪误差 = `日跟踪差异标准差 × sqrt(252)`。
- 默认展示 20、60、250 个交易日；样本不足时不计算较长窗口。
- ETF 与指数必须使用一致的交易日历、复权和分红口径。
- 无法取得全收益指数时，允许使用价格指数，但必须标注口径差异并降低质量评级。

关于全收益指数的替代方案，需要比“降低评级”更强的约束。中证、国证全收益指数序列在现有数据源中没有免费稳定来源，用价格指数替代会系统性地把成分股分红全部计入“正向跟踪差异”。这一偏差的量级取决于标的股息率：对宽基 ETF 约为每年 2% 量级，对红利、银行等高股息主题 ETF 可达 4%–6%，已经远大于真实跟踪误差本身，此时输出的跟踪差异数字不是精度较低，而是不成立。

因此：

- 使用价格指数替代时，只允许输出跟踪误差（差异序列的波动率，受分红影响相对小），不输出跟踪差异的绝对水平。
- 标的年化股息率超过设定阈值时，跟踪质量整节标记为不可用，不降级输出。
- 阶段 0 必须明确回答全收益指数是否可得；不可得则阶段 2 的跟踪质量部分应当缩减，而不是用替代口径凑齐栏目。

## 10. ETF 专用分析流程

### 10.1 分析师组合

1. **技术分析师**
   - K 线、量价、波动率、趋势、支撑阻力。
   - 比较 ETF 与跟踪指数走势，识别明显跟踪偏离。

2. **流动性与交易分析师**
   - 成交额、换手、历史分位、申赎限制；折溢价在阶段 1.5 可用后并入。
   - 严格区分订单资金流与份额资金流。

3. **ETF 结构分析师**
   - 基金资料、规模份额、费率、跟踪指数。
   - 第二阶段增加指数暴露、集中度和跟踪质量。

4. **指数新闻与政策分析师**
   - 指数、行业、政策、调仓和基金公告。

5. **数据质量门控**
   - 先执行确定性检查，再做 LLM 复审。

之后进入模式感知的多空辩论、研究经理、交易员、风险辩论和最终决策。

### 10.2 不再运行的个股分析师

- 个股基本面分析师。
- 个股游资/龙虎榜分析师。
- 解禁与减持监控师。
- 公司社交舆情分析师；可由指数/板块情绪分析替代，但不是第一阶段必需项。

### 10.3 下游决策改造

多空和交易决策提示词必须基于 ETF 事实：

- 多头依据：指数趋势、行业景气、政策支持、估值位置、份额增长、流动性和低跟踪偏离。
- 空头依据：指数估值过高、行业集中、趋势恶化、份额下降、高折溢价、低流动性和跟踪偏离。
- 不得引用公司营收、公司护城河、管理层、董监高、限售解禁或单只成分股 PE 作为 ETF 自身指标。
- 交易约束从 `InstrumentProfile` 读取 T+1、涨跌幅比例和最小交易单位。
- BUY/HOLD/SELL 或五级评级可以继续使用，但结论必须说明数据质量和适用期限。

**提示词改造方式（不得复制提示词文件）**

下游节点的提示词目前把个股假设直接写进正文，例如 bull/bear researcher 的 “advocating for investing in this A-share stock”、trader 的 “Newly listed stocks have no price limit”、aggressive debator 的 “Lockup Expiry / Insider Reduction Report”。改造有三种做法，本方案固定选择第三种：

1. 在提示词内部写 `if 模式` 分支——正文会迅速变得不可读。
2. 复制一份 ETF 版提示词文件——最省事，但两份文案必然随时间漂移，个股侧的后续修正不会同步到 ETF 侧。**明确禁止。**
3. 保持每个节点单一提示词文件，把随模式变化的部分抽成两个注入块：
   - **标的上下文块**：由 `InstrumentProfile` 渲染，说明标的类型、名称、交易所、跟踪指数（如有）、T+1、涨跌幅和最小交易单位。
   - **证据词表**：按模式列出本次可引用的证据类型与对应报告名称，以及明确禁止引用的概念清单。

提示词正文只保留与模式无关的角色与推理要求，所有标的相关事实一律来自注入块。新增分析师或调整能力清单时只改注入块，不动正文。

**工具禁用必须是硬约束**

`interface.route_to_vendor` 会在多个 vendor 之间构建回退链，只有限流异常才中断回退。这意味着如果某个 ETF 分析师被误绑定 `get_fundamentals`，调用不会失败，而是回退到 yfinance 或 Alpha Vantage，用 `510300` 拿回一份看似合理、实则无关的数据。因此仅在 ToolNode 上不注册个股工具是不够的，还需要：

- 在工具包装层加入能力断言：当前 `analysis_capabilities` 不包含该工具所属能力时直接抛错，而不是回退到其他 vendor。
- 该断言必须在 `route_to_vendor` 之前生效，禁止任何形式的静默降级。
- 增加回归测试：ETF 模式下调用 `get_fundamentals`、`get_dragon_tiger_board`、`get_lockup_expiry`、`get_insider_transactions`、`get_profit_forecast`、`get_industry_comparison` 均应抛错，且错误不被上层吞掉。

### 10.4 绩效回顾与记忆

- ETF 历史决策的首选基准是其跟踪指数。
- 指数不可得时才使用明确标注的替代宽基基准。
- 不同跟踪指数的经验不得简单互相迁移。
- 记忆条目保存标的类型、ETF 子类和跟踪指数，避免与同代码旧版个股式分析混淆。

## 11. ETF 专用报告模板

### 11.1 报告头部

- 基金代码、名称、交易所。
- 标的类型：境内股票 ETF。
- 跟踪指数。
- 分析日期和实际使用的最近交易日。
- T+1、涨跌幅限制（标注取自主数据还是推导）、最小交易单位。
- 整体数据可信度。
- 本次实际启用的能力清单。

### 11.2 正文结构

1. 执行摘要与结论。
2. 基金基础资料。
3. 技术面与相对指数表现。
4. 流动性与申赎状态；折溢价在阶段 1.5 可用后并入本节。
5. 份额与规模变化。
6. 指数、行业新闻及政策。
7. 指数暴露与跟踪质量（第二阶段）。
8. 多空论点。
9. 风险管理与最终评级。
10. 数据质量、来源和缺失项。

栏目按运行时能力清单生成：某项能力不在清单内时，对应栏目不出现在正文，而是在第 10 节的“不可得数据”中列出原因。不允许保留空栏目或填入占位文字。

### 11.3 强制风险提示

- 报告仅供研究参考，不构成投资建议。
- IOPV 是参考值，不等于实时基金净值（启用折溢价栏目时适用）。
- 定期报告持仓存在披露滞后。
- 指数权重和 PCF 篮子不等于基金当前完整实际持仓。
- 高折溢价和低流动性可能导致交易价格偏离基金价值。

## 12. 数据质量门控

### 12.1 第一层：程序硬检查

按 ETF 模式动态生成必采清单：

- 证券类型、交易所、ETF 资产类型和跟踪指数已确认。
- 行情所属交易所与 `InstrumentProfile.exchange` 一致。
- 最新行情日期与分析日或最近有效交易日一致。
- 成交额和成交量单位明确。
- 报告中出现的每个栏目都对应能力清单中的一项。
- 折溢价使用同日、同时点可比数据（仅当 `premium_discount` 在能力清单内时检查）。
- 份额和规模均有 `as_of`。
- 新闻、公告具有发布日期和来源。
- 未出现营收、净利润、公司现金流、解禁和董监高等错误必采项。
- 第二阶段的指数权重、PCF、实际持仓明确分栏。
- 实际持仓没有进入短期评分和实时归因。

### 12.2 第二层：时效和一致性检查

- 行情：应为分析日或分析日前最近交易日。
- IOPV：用于折溢价时必须与价格同交易日、同口径。
- PCF：用于当日分析时必须是当日清单。
- 净值、规模和份额：显示实际日期，不假设为实时。
- 指数权重：显示生效日期或供应方日期。
- 定期持仓：始终标记 `stale_reference`，不因刚披露而升级为实时数据。

### 12.3 第三层：LLM 复审

LLM 只检查：

- 报告是否错误混用个股逻辑。
- 结论是否与数据质量相匹配。
- 是否夸大滞后持仓、份额资金流或折溢价含义。
- 风险提示是否覆盖集中度、流动性、跟踪偏离和政策风险。

LLM 不负责补齐缺失原始值，也不能推翻程序判定的数据状态。

### 12.4 评级原则

评级只覆盖分析实际启动之后可能出现的状态。证券类型不明、交易所无法确认等情况在 §7.2 已经阻止运行，不会进入评级，因此不作为评级档位。

- **A**：能力清单内的必采项齐全，关键时点一致，来源完整。
- **B**：少量非关键字段缺失，但不影响主要结论。
- **C**：关键数据滞后，或某项能力因数据源不可用被移出清单，结论只能谨慎参考。
- **D**：核心行情为空或不完整、行情路由与 `InstrumentProfile` 的交易所不一致、折溢价时点不可比却仍被计算。
- **F**：流程误用了个股逻辑——ETF 报告中出现公司财务、解禁、董监高等禁止概念，或滞后持仓进入了短期评分。F 表示流程本身出错，应阻断输出并记录，不只是降级提示。

## 13. 状态、历史和兼容性

### 13.1 新增状态信息

- `instrument_profile`
- `analysis_mode`
- `analysis_capabilities`
- `report_schema_version`
- `etf_profile_report`
- `etf_liquidity_report`
- `etf_exposure_report`
- `data_quality_summary`

是否继续保留旧报告字段可在实现时决定，但不应让 `fundamentals_report` 承载 ETF 结构报告，否则历史展示和语义容易混淆。

### 13.2 历史记录

- 日志文件写入完整 `InstrumentProfile` 和报告版本。
- 为减少前端改动，第一阶段历史列表可以继续只显示“代码 · 日期”；后续可选增加“个股”或“ETF”标签。
- 旧日志缺少 `analysis_mode` 时按旧版个股报告只读处理。
- ETF 与个股报告使用各自的展示和导出模板。

### 13.3 断点与缓存

- 断点身份至少包含代码、日期、分析模式和报告版本。
- 模式或流程版本变化时不恢复旧断点。
- 恢复任务时读取断点中保存的模式与标的信息，不重新执行识别。
- 行情缓存键包含交易所，防止同代码或错误路由污染。
- 指数、PCF、份额和净值缓存分别管理，不使用同一个“最新”时间戳。

## 14. 分阶段实施计划

阶段 -1 与阶段 0 之间、阶段 0 与阶段 1 之间是硬门。阶段 0 的结论有权取消阶段 1.5 和阶段 2 的全部或部分内容。

### 阶段 -1：既有路由缺陷修复（独立于 ETF 模式）

`_get_prefix` 的错误路由和名称映射的过窄正则是当前已存在的缺陷，与是否做 ETF 模式无关，且修复后对现有功能亦有价值。作为独立变更先行合入，不与 ETF 模式共用分支。

- `_get_prefix` 按交易所规则修正：`5xxxxx` 归上交所，`159xxx`/`15xxxx` 归深交所，同时复核 `9`、`8`、`92` 现有分支。
- 名称映射正则放宽以纳入 `5xxxxx` 与 `159xxx`；mootdx `client.stocks(market=...)` 本身已返回这些证券。
- 扩展 `tests/test_market_prefix_routing.py`，加入 ETF 代码用例。
- 消除 `trading_graph.py` Yahoo 路径与 `a_stock.py` 之间关于 `5` 前缀的口径不一致，交易所判定收敛到单一函数。

退出条件：`510300`、`159915` 等代码在腾讯、东财、新浪路径上均落到正确交易所，现有个股与北交所用例不回归。

### 阶段 0：数据源可行性验证（硬门）

目标：用书面结论证明目标范围内的字段确实可以取得，并允许该结论削减后续范围。这是本方案最大的单点风险，不能以“实现时再看”的方式带过。

交付物是一份字段级数据源表，每行至少包含：

| 列 | 说明 |
|---|---|
| 字段 | 如 IOPV、基金份额、指数成分权重 |
| 主源 | 具体接口地址与参数 |
| 备用源 | 同上；无则明确写“无” |
| 是否有历史 | 仅快照 / 有历史序列 / 有限历史 |
| 覆盖度 | 在测试样本上的实测成功率 |
| 限流与稳定性 | 实测限制、封禁风险 |
| 授权风险 | 是否涉及指数公司授权或商用限制 |
| 不可得时的降级行为 | 移出能力清单 / 降级命名 / 整节不输出 |

必须给出明确结论的字段：

- ETF 日行情与成交额（预期可得，需确认 mootdx 对 ETF 代码的覆盖）。
- 基金基础资料：名称、管理人、上市日期、跟踪指数、费率。
- 证券类型与交易所的全量映射表，及其获取与缓存方式。
- 基金份额与基金规模，及其历史序列。
- 基金公告与指数调仓公告。
- IOPV：是否只有实时快照，收盘 IOPV 与当日份额净值的发布时间与来源。
- 每日 PCF 申赎清单：上交所与深交所分别的获取方式、是否有历史。
- 指数成分与权重：来源、更新频率、授权风险。
- 指数全收益序列：是否可得。
- `price_limit_pct` 的推导规则与验证方式。

同时完成：

- 建立证券主数据适配层。
- 在宽基、行业、主题、策略和 20% 涨跌幅 ETF 样本上实测上述字段。

退出条件：数据源表完成并评审；所有测试样本能正确识别类型与交易所且不会进入个股流程；已依据该表书面确认阶段 1、1.5、2 各自实际可交付的范围。IOPV 或 PCF 或指数权重被判定不可得时，直接删除对应阶段内容，不进入实现。

### 阶段 1：基础 ETF 模式

目标：交付一份不会误用个股逻辑、且每个数字都有来源和时点的 ETF 分析。本阶段刻意不包含折溢价——它是整套设计中最容易算错的数字，而一份可用的首版 ETF 报告并不需要它。

- 识别驱动的模式判定、识别结果回显、排除类型拒绝。
- `InstrumentProfile`、`DataPoint` 和能力路由。
- 工具能力断言（禁止 vendor 回退绕过）。
- 技术、流动性、ETF 结构、指数新闻与政策四类报告。
- 行情、成交额、份额规模及公告数据。
- 按能力清单动态登记必采项的质量门控。
- 通过注入块实现的模式感知多空、交易、风险和最终决策提示词。
- ETF 专用报告字段、历史、导出和断点兼容。

退出条件：在份额或公告数据缺失时仍能生成明确降级、无猜测的报告；个股流程回归测试全部通过；ETF 模式下调用任一个股专用工具均抛错。

### 阶段 1.5：净值、IOPV 与折溢价（条件性）

仅在阶段 0 确认存在稳定的 IOPV 或当日份额净值来源时实施。

- `nav_iopv` 与 `premium_discount` 能力及其时点硬校验。
- 当日交易时段与回溯日期的行为差异：回溯日期不输出盘中折溢价。
- 回溯场景下“价格相对最近公布净值偏离”的降级命名与滞后天数展示。
- 折溢价并入流动性报告与报告模板第 4 节。

退出条件：固定样例下折溢价公式与时点一致性测试通过；回溯日期下能力清单中确实不含 `premium_discount`，报告中亦无该栏目。

### 阶段 2：指数暴露与跟踪质量

目标：回答“这只 ETF 暴露什么，以及是否稳定跟踪承诺指数”。

- 指数成分、权重、行业和集中度。
- 指数编制规则及调仓公告。
- 指数聚合估值。
- ETF 与指数跟踪差异和跟踪误差。
- PCF 篮子、现金替代和申赎限制。
- 定期报告实际持仓的历史偏离验证。

退出条件：任何页面和报告都不会把指数权重、PCF 或定期持仓称为当前真实持仓。

### 阶段 3：比较与归因增强

目标：支持同类 ETF 比较，而不是增加虚假的实时持仓精度。

**已交付（阶段 0 约束下的可核验子集，报告 schema `etf-v1.3`）：**

- 同指数不同 ETF 的费率、披露份额/规模、二级市场流动性比较（`peer_comparison` / `etf_compare`）。
- 规则型预警：披露期份额突变、净资产收缩、换手率过低（不含折溢价与跟踪误差）。

**仍关闭（依赖阶段 1.5/2 不可得数据，不实现）：**

- 折溢价与跟踪质量比较。
- 指数风格与行业暴露变化。
- 指数成分对指数收益的公开数据归因。
- 异常折溢价预警。

## 15. 模块级改造范围

| 模块 | 需要的改造 | 阶段 |
|---|---|---|
| `tradingagents/dataflows/a_stock.py` | `_get_prefix` 交易所判定修正、名称映射正则放宽 | -1 |
| `tradingagents/graph/trading_graph.py` | 收敛 Yahoo 与 a_stock 的交易所判定口径 | -1 |
| 证券主数据适配层（新增） | 类型、交易所、基金资料查询与按交易日缓存 | 0 |
| `web/components/sidebar.py` | 识别结果回显、排除类型拒绝、开始按钮门控；不新增勾选框 | 1 |
| `web/app.py` / `web/runner.py` | 将识别得到的模式与冻结的标的信息传入 Graph | 1 |
| `web/progress.py` | 按模式与运行时能力清单动态生成阶段 | 1 |
| `web/components/report_viewer.py` | ETF 专用栏目和术语；栏目按能力清单渲染 | 1 |
| `web/pdf_export.py` | ETF 标题、章节、来源和风险提示 | 1 |
| `web/history.py` | 保存和展示模式、版本、跟踪指数 | 1 |
| `tradingagents/dataflows/a_stock.py` | ETF 行情、成交额、份额规模、基金公告适配 | 1 |
| `tradingagents/dataflows/interface.py` | ETF 数据类别与来源路由；工具能力断言前置于 `route_to_vendor` | 1 |
| `tradingagents/graph/trading_graph.py` | 模式感知工具节点、按跟踪指数取基准、持久化 | 1 |
| `tradingagents/graph/setup.py` | `DEFAULT_ANALYSTS` 改为按模式解析的分析师注册 | 1 |
| `tradingagents/graph/conditional_logic.py` | 支持 ETF 分析师节点 | 1 |
| `tradingagents/graph/propagation.py` | 初始化标的信息、模式、能力清单和新报告字段 | 1 |
| `tradingagents/graph/checkpointer.py` | `thread_id` 纳入模式与报告版本 | 1 |
| `tradingagents/agents/quality_gate.py` | `REPORT_FIELDS` 改为按能力清单登记；关键词禁用检查 | 1 |
| `tradingagents/agents/utils/agent_states.py` | 新增 `instrument_profile`、`analysis_mode` 等状态字段 | 1 |
| 分析师目录 | 新增 ETF 结构、流动性和指数政策分析师 | 1 |
| 多空/交易/风险/经理节点 | 抽出标的上下文块与证据词表注入；正文去个股假设，不复制文件 | 1 |
| `tradingagents/graph/reflection.py` | 基准标签由跟踪指数决定，取消 CSI 300 硬编码 | 1 |
| 折溢价相关模块 | 见阶段 1.5 退出条件 | 1.5 |
| `tradingagents/dataflows/etf_data.py` | 同指数 ETF 发现、费率/规模/流动性比较、份额与换手规则预警 | 3 |
| `tradingagents/agents/analysts/etf_compare_analyst.py` | 同类比较与预警分析师 | 3 |

## 16. 测试方案

### 16.1 标的样本

测试时应从交易所主数据确认样本状态，至少覆盖：

- 沪市宽基 ETF，例如 `510300`。
- 深市宽基或创业板 ETF，例如 `159915`。
- 行业 ETF。
- 主题 ETF。
- 红利或策略 ETF。
- 适用 20% 涨跌幅的股票 ETF。
- 普通 A 股作为个股模式回归样本。
- LOF、债券 ETF、黄金 ETF、跨境 ETF 作为拒绝样本。

### 16.2 必测场景

- 代码输入与中文名称输入。
- 上海 `5xxxxx` 和深圳 `159xxx` 正确路由（阶段 -1 即应覆盖）。
- 输入 ETF 代码时识别为 ETF 并进入 ETF 模式，回显跟踪指数与交易规则。
- 输入普通股票时识别为股票并进入现有个股模式，回显内容不含 ETF 字段。
- 输入跨境、债券、黄金等排除类型时禁用开始并明确拒绝，不降级为个股分析。
- 主数据超时或限流时提示重试；主数据明确返回类型未知时提示不支持。两者都不启动分析。
- 任务恢复时使用任务保存的模式与标的信息，不重新识别。
- 同一代码、同一日期识别模式与旧断点不一致时不恢复旧断点。
- ETF 模式下调用个股专用工具抛错，且不回退到 yfinance / Alpha Vantage。
- 非交易日和未来日期。
- 回溯日期下能力清单不含 `premium_discount`，报告无折溢价栏目（阶段 1.5）。
- 当日 IOPV 缺失或时间戳不匹配（阶段 1.5）。
- 只有 T-1 净值时的降级展示（阶段 1.5）。
- 份额或规模缺失。
- PCF 缺失、现金替代或暂停申赎（阶段 2）。
- 指数权重日期未知（阶段 2）。
- 定期持仓滞后（阶段 2）。
- 高股息标的在仅有价格指数时不输出跟踪差异绝对水平（阶段 2）。
- 旧版个股历史报告加载。
- 同代码同日期存在旧断点但模式或版本变化。
- 任一 ETF 报告中不得出现解禁、董监高、公司营收等错误必采项。
- 同一跟踪指数的两只 ETF 先后分析时，指数级数据与指数分析结果命中缓存而非重复获取。

### 16.3 回归测试

- 现有个股分析流程、报告、历史、暂停恢复和导出不受影响。
- 个股仍使用原有基本面、游资和解禁工具。
- ETF 不得调用这些工具，且调用时抛错而非回退到其他 vendor。
- 质量门控只审核本次实际运行且属于能力清单的报告。
- 提示词注入块改造后，个股模式下渲染出的完整提示词与改造前语义等价。

## 17. 验收标准

### 17.1 功能验收

- 支持范围内样本的证券类型和交易所识别准确，且识别结果在开始分析前对用户可见。
- 排除类型被明确拒绝，不进入个股流程。
- ETF 报告包含基金资料、技术、流动性、份额规模、指数新闻和质量结论；折溢价栏目仅在阶段 1.5 交付且当次可得时出现。
- 多空及最终决策不引用个股财务、解禁和管理层逻辑。
- 历史和导出能还原 ETF 类型及数据时点。

### 17.2 数据验收

- 所有核心值有 `source` 和 `as_of`。
- 沪深路由与交易所主数据一致。
- 份额变化与规模变化在报告中明确区分。
- 折溢价公式和时点一致性通过固定样例测试（阶段 1.5）。
- 第二阶段三类“持仓相关数据”不会混用。
- 每个报告栏目都能对应到能力清单中的一项，不存在无来源栏目。

### 17.3 质量验收

- 数据缺失时不生成猜测值。
- 核心行情或路由错误时质量评级不得高于 D。
- 出现禁止概念或滞后持仓进入短期评分时评级为 F 并阻断输出。
- 定期持仓不得进入短期评分。
- 报告能够明确说明结论基于哪些数据，以及哪些数据不可得。

## 18. 主要风险与应对

| 风险 | 应对措施 |
|---|---|
| **关键数据源根本不存在**（IOPV 历史、PCF、指数权重、全收益指数） | 阶段 0 作为硬门先行验证并出具字段级数据源表，有权直接删除对应阶段，而不是带着未验证假设进入实现 |
| 数据源接口变化或限流 | 适配层、缓存、备用源、健康检查和明确降级 |
| ETF 代码误路由 | 阶段 -1 先修 `_get_prefix`，交易所判定收敛到单一函数，缓存键包含交易所 |
| 识别结果与用户预期不符 | 开始分析前回显识别结果与模式，用户可在开始前发现并修改输入 |
| 主数据查询拖慢输入响应 | 类型映射表整体预取并按交易日缓存，禁止逐只远端查询 |
| IOPV 时点不一致 | 时间戳硬检查，不一致则移出能力清单；回溯日期一律不计算盘中折溢价 |
| 将份额变化误称资金流 | 术语分离，估算值强制标注公式和日期 |
| 滞后持仓造成误导 | 三类数据分栏，实际持仓不参与实时评分 |
| **vendor 回退绕过工具禁用** | 能力断言前置于 `route_to_vendor`，禁用工具直接抛错而非回退到 yfinance / Alpha Vantage |
| LLM 继续使用个股模板 | 注入块化提示词、禁用工具、质量门控关键词检查 |
| **ETF 与个股提示词各自漂移** | 禁止复制提示词文件；单一正文加模式化注入块 |
| 旧断点污染新流程 | 断点键加入模式和报告版本 |
| 指数基准数据不足 | 使用明确标注的替代基准并降低评级，禁止静默替换 |
| 价格指数替代全收益指数造成系统性偏差 | 只输出跟踪误差不输出跟踪差异水平；高股息标的整节不可用 |
| 同指数多只 ETF 重复消耗 | 指数级数据与指数分析按 `tracking_index_code` 缓存复用 |

## 19. 实施决策摘要

本方案固定以下决定：

1. 当前只支持境内被动股票 ETF。
2. 分析模式由证券主数据识别决定，不设模式勾选框；识别结果在开始分析前回显，用户点击开始即为确认。
3. `_get_prefix` 路由缺陷与名称映射正则作为独立缺陷先行修复（阶段 -1），不并入 ETF 模式交付。
4. 阶段 0 是硬门，交付字段级数据源表，并有权据此删减阶段 1.5 与阶段 2 的范围。
5. 折溢价与 IOPV 移出阶段 1，单列为条件性的阶段 1.5。
6. `InstrumentProfile` 区分身份字段与规则字段：身份字段缺失即终止，规则字段允许推导并标注状态。
7. ETF 使用独立报告字段和模板，不复用“基本面报告”名称。
8. 提示词按模式注入上下文块与证据词表，禁止复制出 ETF 版提示词文件。
9. 工具禁用通过能力断言实现，必须前置于 vendor 回退链。
10. 第一阶段必须同时改造下游决策链，不能只替换前端分析师。
11. 第二阶段保留，但改为“指数暴露与跟踪质量”，不承诺实时基金持仓。
12. 指数权重、PCF 和定期报告持仓必须分开保存和展示。
13. 定期报告持仓不参与短期交易评分。
14. ETF 绩效回顾优先使用其跟踪指数；指数级数据与分析按跟踪指数缓存复用。
15. 数据不可得时移出能力清单并在报告中说明，不由模型补全，也不保留空栏目。

## 20. 官方规则与数据参考

- 中国证监会：《公开募集证券投资基金信息披露管理办法》  
  https://www.csrc.gov.cn/csrc/c106256/c1653985/content.shtml
- 上海证券交易所：ETF 交易规则常见问题  
  https://etf.sse.com.cn/fund/quertion/
- 上海证券交易所：ETF 公告申购赎回清单  
  https://www.sse.com.cn/assortment/fund/list/etfinfo/redemptionlist/
- 上海证券交易所：《上海证券交易所交易规则（2026 年修订）》  
  https://www.sse.com.cn/lawandrules/sselawsrules2025/fund/trading/c/c_20260424_10817739.shtml
- 深圳证券交易所：20% 涨跌幅基金说明  
  https://www.szse.cn/www/investor/knowledge/t20201118_583202.html

## 21. 完成定义

阶段 1 的完成定义：

- 分析模式完全由证券主数据识别产生，界面无模式勾选框。
- 识别结果在开始分析前回显，包含类型、名称、交易所，ETF 另含跟踪指数与交易规则。
- 排除类型和无法确认类型均阻止运行并给出明确原因，不降级为个股分析。
- 证券主数据、交易所和产品类型识别可靠，且不拖慢输入响应。
- ETF 不再调用个股财务、龙虎榜和解禁工具，误调用时抛错而非回退。
- 全部下游节点通过注入块理解 ETF 交易规则和数据语义，且不存在复制出的 ETF 版提示词文件。
- 份额规模和指数数据具备可审计的来源及时点。
- 报告栏目与运行时能力清单一致，不可得数据在“数据质量”一节说明而非留空。
- 质量门控能阻止口径错误进入最终结论。
- 暂停恢复和历史重载使用任务保存的模式与标的信息，不重新识别。
- 个股模式和历史兼容性通过回归测试。

阶段 1.5 与阶段 2 的完成定义以各自退出条件为准，且以阶段 0 数据源表判定为可交付为前提。若阶段 0 判定相关数据不可得，对应阶段视为已关闭，而非未完成。
